# Boot without interaction

- Bios
    - Boot options
        - Boot retry → legacy
    - Boot order
        - Network boot [LEGACY MODE]
            - Intel boot (option 1)
            - Disabled (option 2)